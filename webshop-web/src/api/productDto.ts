export type ProductDto = {
    id: string;
    name: string;
    description: string;
    price: number;
    image: string;
    sku: string;
    basePrice: number;
    sellPrice: number;
    inStock: boolean;
    createdAt: string;
    updatedAt: string;
}

